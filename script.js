// const txtElement = ['Pelajar',' Pemula' ];
// let count = 0;
// let textIndex = 0;
// let currenTxt = '';
// let words = '';

let typed = new Typed(".ketikan", {
  strings: ["  Saya Zaki dan saya adalah seorang Pelajar"],
  typeSpeed: 200,
  backDelay: 500,
  loop: true,
});

// (function ngetik(){

//     if(count == txtElement.length){
//         count == 0;
//     }
//      currenTxt = txtElement[count];

//      words = currenTxt.slice(0, ++textIndex);
//      document.querySelector('.ketikan').textContent = words;

//      if(words.length == currenTxt.length){
//         count++;
//         textIndex = 0;
//      }

//      setTimeout(ngetik, 400);

// })();
